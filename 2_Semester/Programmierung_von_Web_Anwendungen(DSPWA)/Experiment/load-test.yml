config:
  target: 'http://localhost:3000'  # Basis-URL deiner API
  phases:
    - duration: 60
      arrivalRate: 2000
  defaults:
    headers:
      Content-Type: 'application/json'

scenarios:
  - flow:
      - get:
          url: '/api/synchron'  # Erster Endpunkt testen
